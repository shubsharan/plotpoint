# Material failure fixtures

Each directory isolates one expressible invalid material boundary. The closed project configuration
has no content-to-content or component-to-component link field, so a declarative material reference
cycle cannot be represented. Progression rule cycles are covered by progression validation instead;
static ESM cycles remain allowed by the project contract.
