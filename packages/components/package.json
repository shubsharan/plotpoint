{
  "name": "@packages/components",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src/",
    "format": "oxfmt src/",
    "test": "vitest run"
  },
  "devDependencies": {
    "@packages/config": "workspace:*"
  }
}
