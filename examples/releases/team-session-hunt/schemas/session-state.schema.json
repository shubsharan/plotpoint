{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "hunt.session-state.v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["activeRound", "status"],
  "properties": {
    "activeRound": { "type": "integer", "minimum": 0 },
    "status": { "enum": ["briefing", "running", "complete"] }
  }
}
