{
  "name": "plotpoint",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=25.4.0"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "oxlint -c packages/config/oxlint.json .",
    "lint:fix": "oxlint -c packages/config/oxlint.json --fix .",
    "format": "oxfmt --check",
    "format:fix": "oxfmt ."
  },
  "devDependencies": {
    "oxfmt": "^0.38.0",
    "oxlint": "^1.53.0",
    "oxlint-tsgolint": "^0.16.0",
    "tsdown": "^0.21.2",
    "turbo": "^2.8.16",
    "typescript": "6.0.1-rc",
    "vitest": "^4.0.18"
  },
  "packageManager": "pnpm@10.32.1"
}
