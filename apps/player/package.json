{
  "name": "@plotpoint/player",
  "version": "0.0.0",
  "private": true,
  "description": "Plotpoint native player host and embedded web shell.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check-types": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@plotpoint/modules": "workspace:*",
    "@plotpoint/protocol": "workspace:*",
    "@plotpoint/runtime": "workspace:*"
  },
  "devDependencies": {
    "@plotpoint/typescript-config": "workspace:*"
  }
}
