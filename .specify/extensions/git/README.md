# Git Extension

Feature artifacts and branches share one name:

```text
docs/features/0007-budget-policy/
feature/0007-budget-policy
```

The feature command allocates the next four-digit number from `docs/features/`. Automatic commits
remain disabled in `git-config.yml`.
