# Validate the Spec Kit workflow

Run `.specify/scripts/bash/check-workflow.sh`. Before implementation, any Major architecture impact
must reference the same Accepted ADRs from both the feature spec and plan. Done features require
authenticated `gh` access so their merged PR can be verified.
